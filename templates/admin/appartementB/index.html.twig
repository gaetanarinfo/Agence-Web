{% extends "base.html.twig" %}

{% block title 'Gérer les appartements' %}

{% block body %}

    <div class="container mt-4 mb-4 w-50">

        <p class="title-cont">Gérer les appartements</p>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success text-center">
                <i class="fa fa-check text-success" aria-hidden="true"></i> {{ message }}
            </div>
        {% endfor %}

        {% for message in app.flashes('error') %}
            <div class="alert alert-danger text-center">
                <i class="fas fa-times text-danger" aria-hidden="true"></i> {{ message }}
            </div>
        {% endfor %}

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Titre</th>
                    <th>Disponibilité</th>
                    <th style="text-align: center;">Action</th>
                </tr>    
            </thead>
            <tbody>
            {% for appartementBs in appartementB %}
            {% if appartementBs.createdBy == app.user.username %}
                <tr>
                    <td style="vertical-align: middle;">{{ appartementBs.title }}</td>
                    <td style="vertical-align: middle;">{% if appartementBs.available == 0 %}<i class="fas fa-check text-success"></i> Disponible{% else %}<i class="fas fa-times text-danger"></i> Vendu{% endif %}</td>
                    <td style="vertical-align: middle; text-align: center;">
                        <a href="{{ path('admin.appartementB.edit', {id: appartementBs.id}) }}" class="btn btn-secondary">Editer</a>
                        <form method="post" action="{{ path('admin.appartementB.delete', {id: appartementBs.id}) }}" style="display: inline-block;">
                            <input type="hidden" name="_method" value="DELETE" />
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appartementBs.id) }}" />
                            <button class="btn btn-danger">Supprimer</button>
                        </form>
                    </td>
                </tr>
            {% endif %}
            {% endfor %}
            </tbody>
        </table>

        <div class="row mt-4">
            <div class="col-12 col-md-12 col-xl-12" style="display: flex;justify-content: space-around;">
                {{ knp_pagination_render(appartementB) }}
            </div>
        </div>

        <div class="text-right">
            <a href='{{ path('admin.appartementB.new') }}' class="btn btn-primary">Crée un appartement</a>
        </div>

    </div>
{% endblock %}