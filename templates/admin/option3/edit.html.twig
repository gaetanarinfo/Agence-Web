{% extends "base.html.twig" %}

{% block body %}

    <div class="container mt-4 mb-4 w-25">
        <p class="title-cont w-50 text-center m-auto mb-2">Editer l'option</p>
        <hr class="w-50 m-auto mb-2" />
        {{ include('admin/option3/_form.html.twig', {button: 'Editer'}) }}
    </div>

{% endblock %}